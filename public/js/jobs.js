// index.js or separate JavaScript file
document.getElementById('jobs-link').addEventListener('click', () => {
    // Make AJAX request to fetch jobs data
    // fetch('/jobs')
    //   .then(response => response.json())
    //   .then(jobs => {
    //     // Call a function to display the jobs data
    //     //console.log(jobs)
    //     displayJobs(jobs);
    //   })
    //   .catch(error => {
    //     //console.log('Error:', error);
    //   });
  });
  

  // index.js or separate JavaScript file
 function displayJobs(jobs) {
//     const jobsContainer = document.getElementById('jobs-container');
  
//     // Clear existing job cards
//     jobsContainer.innerHTML = '';
  
//     // Loop through the jobs array and create HTML cards
//     jobs.forEach(job => {
//       const jobCard = document.createElement('div');
//       jobCard.classList.add('job-card'); // Apply appropriate CSS class to style the card
  
//       // Create HTML elements to display job information
//       const titleElement = document.createElement('h2');
//       titleElement.textContent = job.title;
  
//       const descriptionElement = document.createElement('p');
//       descriptionElement.textContent = job.description;
  
//       // Append job information to the card
//       jobCard.appendChild(titleElement);
//       jobCard.appendChild(descriptionElement);
  
//       // Append the job card to the container
//       jobsContainer.appendChild(jobCard);
//     });
   }
  